## Packages
framer-motion | Animations for smooth transitions and keypad interactions
lucide-react | Iconography for UI elements
clsx | Utility for conditional class names
tailwind-merge | Utility for merging tailwind classes

## Notes
The app mimics a JEE Main exam interface.
Backend routes are defined in shared/routes.ts and use Zod schemas for validation.
Timer logic needs to handle auto-submission when time runs out.
Keypad interaction should support appending text to the input field.
